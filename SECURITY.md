# Security

Do not commit credentials.

Connection profiles name environment variables only.

The browser studio uses the trusted demonstration catalog.

It does not accept file paths or service addresses from the browser.

Protected audit files use mode `0600`.

Report a security fault through a private repository message.

Do not include a real token or private baseball record in that message.
